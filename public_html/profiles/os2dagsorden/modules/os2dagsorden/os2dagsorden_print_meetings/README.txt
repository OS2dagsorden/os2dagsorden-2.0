The purpose of this method is to present the meetings list in a calendar view (half a year is used as a timeframe).

The module also support printing the calendar with meetings, where the print-oriendted (lightweight) version is served to the user.
The print itself is handeled with "print" module.

