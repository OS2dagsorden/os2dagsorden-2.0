Th epurpose of this module is to gather all the special functions that are user all over the project. 

These funtions include:
-permissons check (where the Drupal permission API cannot do it)
-indicators check (show hide a specific indicator)
-security check (e.g. a given bullet point is a part of the given meeting)
-getting user's IP address (and stroing it, if a user accesses a particular part of the application)
-scheduling maintenance jobs (deleting expired user rights/roles)
